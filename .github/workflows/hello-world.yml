name: Hello World
on:
  push:
    branches: 
      - main
  workflow_dispatch: 
    inputs:
      name:
        description: 'Name'
        required: true
        default: 'Komal'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello World"
